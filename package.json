{
  "name": "eraw",
  "private": true,
  "version": "0.2.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "check": "tsc --noEmit",
    "test:i18n": "node --test tests/i18n.test.mjs",
    "test:frontend": "node --test tests/i18n.test.mjs tests/descriptor-input.test.mjs",
    "release": "tauri build --no-bundle",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "~5.8.0",
    "vite": "^7.0.0"
  }
}
